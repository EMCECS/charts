---
apiVersion: v1
kind: Service
metadata:
  name: fedsvc
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fedsvc.labels" . | nindent 4 }}
  annotations:
    objectscale.dellemc.com/federationRegistration: "ObjectScale"
spec:
  {{- if eq .Values.objectscaleCommunicationGateway.enabled true }}
  type: {{ .Values.fedsvc.service.clusterIpType }}
  {{- else }}
  type: {{ .Values.fedsvc.service.loadBalancerType }}
  {{- end }}
  ports:
    - port: {{ .Values.fedsvc.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fedsvc.selectorLabels" . | nindent 4 }}
