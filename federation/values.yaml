---
tag: 0.68.2
global:
  registry: emccorp
  # Default: true == Watch ALL namespaces
  watchAllNamespaces: true
  # Platform:
  platform: Default

objectscaleCommunicationGateway:
  enabled: false

fedsvc:
  image:
    pullPolicy: IfNotPresent
    repository: fedsvc
    # tag: stable
  livenessProbe:
    probePath: /fedsvchealthcheck
  readinessProbe:
    probePath: /fedsvchealthcheck
  replicaCount: 3
  service:
    port: 9500
    clusterIpType: ClusterIP
    loadBalancerType: LoadBalancer
