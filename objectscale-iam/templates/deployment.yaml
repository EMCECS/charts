---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: objectscale-iam
  labels:
    {{- include "iam.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.iam.replicaCount }}
  selector:
    matchLabels:
      {{- include "iam.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "iam.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: objectscale-iam
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      containers:
      - name: iam
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "atlas,flex,dns"
        - name: ATLAS_SERVICE_HOSTNAME
          value: "{{ .Release.Name }}-iam-atlas-client"
        - name: MY_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: MY_POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: MY_POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        image: {{ default  .Values.global.registry .Values.iam.image.registry }}/{{ .Values.iam.image.repository }}:{{ default .Values.tag .Values.iam.image.tag }}
        imagePullPolicy: {{ .Values.iam.image.pullPolicy }}
        ports:
          - name: http
            containerPort: 9401 
            protocol: TCP
        livenessProbe:
            httpGet:
                path: {{ .Values.iam.livenessProbe.probePath }}
                port: http
            failureThreshold: {{ .Values.iam.livenessProbe.failureThreshold | default 3 }}
            periodSeconds: {{ .Values.iam.livenessProbe.periodSeconds | default 60 }}
        readinessProbe:
            httpGet:
                path: {{ .Values.iam.readinessProbe.probePath }}
                port: http
            failureThreshold: {{ .Values.iam.readinessProbe.failureThreshold | default 3 }}
            periodSeconds: {{ .Values.iam.readinessProbe.periodSeconds | default 60 }}


